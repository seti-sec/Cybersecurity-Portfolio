index=wineventlog EventCode=4769 TicketEncryptionType=0x17
| stats count as kerberos_requests
  values(Service_Name) as services 
  values(Account_Name) as users
  by Client_Address,_time
| where kerberos_requests >= 5
